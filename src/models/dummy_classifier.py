# REMOVED: DummyECGClassifier replaced by XResNet1D-101 as the project baseline.
# A naive frequency-prior baseline is available via src/evaluation/metrics.py if needed.
raise ImportError(
    "DummyECGClassifier is no longer used. See src/evaluation/metrics.py for baseline comparisons."
)
